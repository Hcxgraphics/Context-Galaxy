[{"AllowMultiple":false,"EndLine":49,"ReplacementContent":"class ContextNodeUpdate(BaseModel):\n    label: str | None = None\n    priority: str | None = None\n    summary: str | None = None\n    is_active: bool | None = None\n\nclass CustomNodeCreate(BaseModel):\n    label: str\n    summary: str\n    priority: str = \"medium\"","StartLine":44,"TargetContent":"class ContextNodeUpdate(BaseModel):\n    label: str | None = None\n    priority: str | None = None\n    summary: str | None = None\n    is_active: bool | None = None"},{"AllowMultiple":false,"EndLine":126,"ReplacementContent":"@router.post(\"/{chat_id}/node\")\nasync def create_custom_node(\n    chat_id: str,\n    payload: CustomNodeCreate,\n    db: AsyncSession = Depends(get_db)\n):\n    from app.utils.embeddings import EmbeddingService\n    \n    try:\n        chat_uuid = UUID(chat_id)\n    except ValueError:\n        raise HTTPException(status_code=400, detail=\"Invalid chat ID format\")\n        \n    # Check if a root node exists\n    root_node_query = await db.execute(\n        select(ContextNode).where(\n            ContextNode.chat_id == chat_uuid,\n            ContextNode.node_type == \"ROOT\"\n        )\n    )\n    root_node = root_node_query.scalars().first()\n    parent_id = root_node.id if root_node else None\n    depth_level = 2.0 if root_node else 1.0\n    \n    topic_embedding = EmbeddingService.get_embedding(payload.label)\n    \n    new_node = ContextNode(\n        chat_id=chat_uuid,\n        parent_id=parent_id,\n        label=payload.label,\n        node_type=\"subtopic\",\n        priority=payload.priority.lower(),\n        created_by=\"user\",  # Marked as user-created\n        frequency_score=1.0,\n        semantic_score=1.0,\n        depth_score=1.0 / (depth_level + 1.0),\n        activation_score=1.0,\n        is_active=True,\n        summary=payload.summary,\n        embedding=topic_embedding,\n        depth_level=depth_level\n    )\n    db.add(new_node)\n    await db.flush()\n    \n    # Create orbit edge if root exists\n    if roo
<truncated 637 bytes>